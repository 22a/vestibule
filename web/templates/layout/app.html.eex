<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Vestibule</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
    <link rel="shortcut icon" href="<%= static_path(@conn, "/images/vestibule_64.png") %>" type="image/x-icon" />
  </head>

  <body>
    <div class="container">
      <nav class="navbar navbar-default">
        <div class="container-fluid">
          <!-- Brand and toggle get grouped for better mobile display -->
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">
              <img alt="Vestibule" src="/images/vestibule_1024.png" class="vestibule-logo">
            </a>
            <a class="navbar-brand" href="/">
              Vestibule
            </a>
          </div>
          <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
              <li>
                <%= link("Contests", to: contest_path(@conn, :index)) %>
              </li>
              <li>
                <%= link("Problems", to: problem_path(@conn, :index)) %>
              </li>
              <li>
                <%= link("Attempts", to: attempt_path(@conn, :index)) %>
              </li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                  <%= if Coherence.current_user(@conn) do %>
                    <%= Coherence.current_user(@conn).email %>
                    <span class="caret"></span>
                  </a>
                  <ul class="dropdown-menu">
                    <li><%= Vestibule.Coherence.ViewHelpers.profile_link(@conn, Coherence.current_user(@conn), "Manage Account") %></li>
                    <li role="separator" class="divider"></li>
                    <li><%= Vestibule.Coherence.ViewHelpers.signout_link(@conn, "Sign Out", "signout-link") %></li>
                  </ul>
                </li>
              <% else %>
                Sign In / Register
                <span class="caret"></span>
              </a>
              <ul class="dropdown-menu">
                <li><a href="/sessions/new">Sign In</a></li>
                <li role="separator" class="divider"></li>
                <li><%= Vestibule.Coherence.ViewHelpers.register_link(@conn, "Register") %></li>
              </ul>
            <% end %>
          </li>
        </ul>
          </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
      </nav>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
    <script src="https://code.jquery.com/jquery-3.1.1.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
  </body>
</html>
